package units.platforms;

public class NormalPlatform extends Platform {
    public NormalPlatform(double x, double y, String url, int velocityX, int velocityY) {
        super(x, y, url, velocityX, velocityY);
    };
}
